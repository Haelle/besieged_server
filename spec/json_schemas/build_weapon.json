{
  "type": "object",
  "title": "Build a siege Weapon",
  "required": ["camp", "siege_machine", "status"],
  "properties": {
    "camp": {
      "$ref": "camp.json"
    },
    "siege_machine": {
      "$ref": "siege_machine.json"
    },
    "status": {
      "type": "string",
      "enum": ["built"]
    }
  }
}
